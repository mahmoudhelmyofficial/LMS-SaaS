@model LMS.Models.InstructorApplicationViewModel

@{
    ViewData["Title"] = Html.T("التقديم كمدرس", "Apply as Instructor");
    Layout = "~/Views/Shared/_ErrorDashboardLayout.cshtml";
}

@section Styles {
    <style>
        :root {
            --primary: #004aad;
            --primary-dark: #003380;
            --primary-light: #0066cc;
            --gradient-primary: linear-gradient(135deg, #004aad 0%, #0066cc 50%, #3385d6 100%);
        }
        .apply-hero {
            background: var(--gradient-primary);
            color: white;
            padding: 40px 0;
            margin: -1rem -1.5rem 2rem -1.5rem;
            text-align: center;
            border-radius: 0 0 12px 12px;
        }
        .apply-hero h1 { font-size: 1.75rem; font-weight: 700; margin-bottom: 0.5rem; }
        .apply-hero p { font-size: 1rem; opacity: 0.9; max-width: 600px; margin: 0 auto; }
        .apply-card {
            background: white;
            border-radius: 12px;
            box-shadow: 0 4px 15px rgba(0,0,0,.08);
            padding: 1.5rem;
            margin-bottom: 1.5rem;
            border: 1px solid var(--border-color, #e2e8f0);
        }
        .section-title {
            font-size: 1.15rem;
            font-weight: 700;
            color: #333;
            margin-bottom: 1rem;
            padding-bottom: 0.5rem;
            border-bottom: 3px solid var(--primary);
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .section-title i { color: var(--primary); }
        .form-label { font-weight: 600; color: #374151; margin-bottom: 6px; }
        .apply-card .form-control, .apply-card .form-select {
            border-radius: 8px;
            padding: 10px 14px;
            border: 2px solid #e5e7eb;
            transition: all 0.2s ease;
        }
        .apply-card .form-control:focus, .apply-card .form-select:focus {
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(0, 74, 173, 0.1);
        }
        .form-text { color: #6b7280; font-size: 0.85rem; margin-top: 4px; }
        .apply-card .btn-primary {
            background: var(--gradient-primary);
            border: none;
            padding: 12px 32px;
            font-weight: 600;
            border-radius: 8px;
            transition: all 0.2s ease;
        }
        .apply-card .btn-primary:hover { box-shadow: 0 4px 12px rgba(0, 74, 173, 0.35); }
        .benefits-card {
            background: linear-gradient(135deg, #f0f9ff 0%, #e0f2fe 100%);
            border-radius: 12px;
            padding: 1.25rem;
            margin-bottom: 1.5rem;
            border: 1px solid rgba(0, 74, 173, 0.1);
        }
        .benefits-card h5 { color: var(--primary); font-weight: 700; margin-bottom: 1rem; font-size: 1.1rem; }
        .benefit-item { display: flex; align-items: start; gap: 10px; margin-bottom: 0.75rem; }
        .benefit-item i { color: #10b981; font-size: 1.1rem; margin-top: 2px; }
        .benefit-item span { color: #374151; font-size: 0.95rem; }
        .required-mark { color: #ef4444; margin-right: 2px; }
        .apply-page-wrapper .alert { border-radius: 8px; border: none; }
        .apply-page-wrapper .alert-success { background-color: #d1fae5; color: #065f46; }
        .apply-page-wrapper .alert-danger { background-color: #fee2e2; color: #991b1b; }
        .apply-page-wrapper .alert-info { background-color: #dbeafe; color: #1e40af; }
    </style>
}

<div class="apply-page-wrapper">
<!-- Hero Section -->
<div class="apply-hero">
    <div class="container-fluid px-0">
        <h1>🎓 @Html.T("انضم إلى فريق المدرسين", "Join Our Instructor Team")</h1>
        <p>@Html.T("شارك خبراتك ومعرفتك مع آلاف الطلاب وابدأ رحلتك في التدريس عبر الإنترنت", "Share your expertise and knowledge with thousands of students and start your online teaching journey")</p>
    </div>
</div>

<div class="container-fluid px-0">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <!-- Benefits Card -->
            <div class="benefits-card">
                <h5><i class="feather-star me-2"></i>@Html.T("مميزات الانضمام كمدرس", "Benefits of Joining as Instructor")</h5>
                <div class="row">
                    <div class="col-md-6">
                        <div class="benefit-item">
                            <i class="feather-check-circle"></i>
                            <span>@Html.T("دخل إضافي من بيع دوراتك", "Extra income from selling your courses")</span>
                        </div>
                        <div class="benefit-item">
                            <i class="feather-check-circle"></i>
                            <span>@Html.T("وصول لآلاف الطلاب", "Reach thousands of students")</span>
                        </div>
                        <div class="benefit-item">
                            <i class="feather-check-circle"></i>
                            <span>@Html.T("أدوات احترافية لإنشاء المحتوى", "Professional tools for content creation")</span>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="benefit-item">
                            <i class="feather-check-circle"></i>
                            <span>@Html.T("دعم فني على مدار الساعة", "24/7 technical support")</span>
                        </div>
                        <div class="benefit-item">
                            <i class="feather-check-circle"></i>
                            <span>@Html.T("تحليلات مفصلة لأداء دوراتك", "Detailed analytics for your courses")</span>
                        </div>
                        <div class="benefit-item">
                            <i class="feather-check-circle"></i>
                            <span>@Html.T("مجتمع من المدرسين المحترفين", "Community of professional instructors")</span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Alert Messages -->
            @if (TempData["SuccessMessage"] != null)
            {
                <div class="alert alert-success">
                    <i class="feather-check-circle me-2"></i>
                    @TempData["SuccessMessage"]
                </div>
            }
            @if (TempData["ErrorMessage"] != null)
            {
                <div class="alert alert-danger">
                    <i class="feather-alert-circle me-2"></i>
                    @TempData["ErrorMessage"]
                </div>
            }
            @if (TempData["InfoMessage"] != null)
            {
                <div class="alert alert-info">
                    <i class="feather-info me-2"></i>
                    @TempData["InfoMessage"]
                </div>
            }

            <!-- Application Form -->
            <div class="apply-card">
                <form asp-controller="Home" asp-action="ApplyAsInstructor" method="post">
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="AcceptTerms" value="false" />

                    @if (!ViewContext.ModelState.IsValid)
                    {
                        <div class="alert alert-danger mb-4">
                            <strong>@Html.T("يرجى تصحيح الأخطاء التالية:", "Please correct the following errors:")</strong>
                            <ul class="mb-0 mt-2">
                                @foreach (var error in ViewContext.ModelState.Values.SelectMany(v => v.Errors))
                                {
                                    <li>@error.ErrorMessage</li>
                                }
                            </ul>
                        </div>
                    }

                    <!-- Personal Information -->
                    <h5 class="section-title">
                        <i class="feather-user"></i>
                        @Html.T("المعلومات الشخصية", "Personal Information")
                    </h5>
                    
                    <div class="row mb-4">
                        <div class="col-md-6 mb-3">
                            <label asp-for="FirstName" class="form-label">
                                <span class="required-mark">*</span>@Html.T("الاسم الأول", "First Name")
                            </label>
                            <input asp-for="FirstName" class="form-control" placeholder="@Html.T("أدخل اسمك الأول", "Enter your first name")" />
                            <span asp-validation-for="FirstName" class="text-danger small"></span>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label asp-for="LastName" class="form-label">
                                <span class="required-mark">*</span>@Html.T("الاسم الأخير", "Last Name")
                            </label>
                            <input asp-for="LastName" class="form-control" placeholder="@Html.T("أدخل اسمك الأخير", "Enter your last name")" />
                            <span asp-validation-for="LastName" class="text-danger small"></span>
                        </div>
                    </div>

                    <div class="row mb-4">
                        <div class="col-md-6 mb-3">
                            <label asp-for="Email" class="form-label">
                                <span class="required-mark">*</span>@Html.T("البريد الإلكتروني", "Email")
                            </label>
                            <input asp-for="Email" class="form-control" type="email" placeholder="example@email.com" />
                            <span asp-validation-for="Email" class="text-danger small"></span>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label asp-for="PhoneNumber" class="form-label">@Html.T("رقم الهاتف", "Phone Number")</label>
                            <input asp-for="PhoneNumber" class="form-control" placeholder="+966 5xx xxx xxxx" />
                            <span asp-validation-for="PhoneNumber" class="text-danger small"></span>
                        </div>
                    </div>

                    @if (!User.Identity?.IsAuthenticated ?? true)
                    {
                        <div class="row mb-4">
                            <div class="col-md-6 mb-3">
                                <label asp-for="Password" class="form-label">
                                    <span class="required-mark">*</span>@Html.T("كلمة المرور", "Password")
                                </label>
                                <input asp-for="Password" class="form-control" type="password" placeholder="@Html.T("8 أحرف على الأقل", "At least 8 characters")" />
                                <span asp-validation-for="Password" class="text-danger small"></span>
                                <div class="form-text">@Html.T("يجب أن تحتوي على حرف كبير وحرف صغير ورقم", "Must contain an uppercase letter, a lowercase letter, and a number")</div>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label asp-for="ConfirmPassword" class="form-label">
                                    <span class="required-mark">*</span>@Html.T("تأكيد كلمة المرور", "Confirm Password")
                                </label>
                                <input asp-for="ConfirmPassword" class="form-control" type="password" placeholder="@Html.T("أعد إدخال كلمة المرور", "Re-enter password")" />
                                <span asp-validation-for="ConfirmPassword" class="text-danger small"></span>
                            </div>
                        </div>
                    }

                    <div class="row mb-4">
                        <div class="col-md-6 mb-3">
                            <label asp-for="Country" class="form-label">@Html.T("الدولة", "Country")</label>
                            <select asp-for="Country" class="form-select">
                                <option value="">@Html.T("اختر الدولة", "Select Country")</option>
                                <option value="المملكة العربية السعودية">المملكة العربية السعودية</option>
                                <option value="مصر">مصر</option>
                                <option value="الإمارات">الإمارات</option>
                                <option value="الكويت">الكويت</option>
                                <option value="قطر">قطر</option>
                                <option value="البحرين">البحرين</option>
                                <option value="عمان">عمان</option>
                                <option value="الأردن">الأردن</option>
                                <option value="لبنان">لبنان</option>
                                <option value="العراق">العراق</option>
                                <option value="المغرب">المغرب</option>
                                <option value="تونس">تونس</option>
                                <option value="الجزائر">الجزائر</option>
                                <option value="فلسطين">فلسطين</option>
                                <option value="السودان">السودان</option>
                                <option value="ليبيا">ليبيا</option>
                                <option value="اليمن">اليمن</option>
                                <option value="سوريا">سوريا</option>
                                <option value="أخرى">أخرى</option>
                            </select>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label asp-for="City" class="form-label">@Html.T("المدينة", "City")</label>
                            <input asp-for="City" class="form-control" placeholder="@Html.T("أدخل مدينتك", "Enter your city")" />
                        </div>
                    </div>

                    <!-- Professional Information -->
                    <h5 class="section-title mt-5">
                        <i class="feather-briefcase"></i>
                        @Html.T("المعلومات المهنية", "Professional Information")
                    </h5>

                    <div class="row mb-4">
                        <div class="col-md-6 mb-3">
                            <label asp-for="Specialization" class="form-label">
                                <span class="required-mark">*</span>@Html.T("التخصص", "Specialization")
                            </label>
                            <input asp-for="Specialization" class="form-control" placeholder="@Html.T("مثال: تطوير الويب، التسويق الرقمي", "e.g. Web Development, Digital Marketing")" />
                            <span asp-validation-for="Specialization" class="text-danger small"></span>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label asp-for="YearsOfExperience" class="form-label">
                                <span class="required-mark">*</span>@Html.T("سنوات الخبرة", "Years of Experience")
                            </label>
                            <input asp-for="YearsOfExperience" class="form-control" type="number" min="0" max="50" placeholder="@Html.T("عدد سنوات الخبرة", "Number of years")" />
                            <span asp-validation-for="YearsOfExperience" class="text-danger small"></span>
                        </div>
                    </div>

                    <div class="mb-4">
                        <label asp-for="Education" class="form-label">@Html.T("المؤهل العلمي", "Education")</label>
                        <input asp-for="Education" class="form-control" placeholder="@Html.T("مثال: بكالوريوس علوم الحاسب", "e.g. BSc Computer Science")" />
                    </div>

                    <div class="mb-4">
                        <label asp-for="Bio" class="form-label">@Html.T("نبذة عنك", "About You")</label>
                        <textarea asp-for="Bio" class="form-control" rows="4" placeholder="@Html.T("اكتب نبذة مختصرة عن نفسك وخبراتك...", "Write a brief bio about yourself and your experience...")"></textarea>
                        <span asp-validation-for="Bio" class="text-danger small"></span>
                    </div>

                    <div class="mb-4">
                        <label asp-for="WhyTeach" class="form-label">@Html.T("لماذا تريد التدريس معنا؟", "Why do you want to teach with us?")</label>
                        <textarea asp-for="WhyTeach" class="form-control" rows="3" placeholder="@Html.T("أخبرنا عن دوافعك للانضمام كمدرس...", "Tell us about your motivation to join as an instructor...")"></textarea>
                    </div>

                    <div class="mb-4">
                        <label asp-for="ProposedTopics" class="form-label">@Html.T("المواضيع التي تود تدريسها", "Topics You Want to Teach")</label>
                        <textarea asp-for="ProposedTopics" class="form-control" rows="3" placeholder="@Html.T("اذكر المواضيع أو الدورات التي تخطط لإنشائها...", "List the topics or courses you plan to create...")"></textarea>
                    </div>

                    <!-- Social Links -->
                    <h5 class="section-title mt-5">
                        <i class="feather-link"></i>
                        @Html.T("الروابط (اختياري)", "Links (optional)")
                    </h5>

                    <div class="row mb-4">
                        <div class="col-md-6 mb-3">
                            <label asp-for="LinkedInUrl" class="form-label">
                                <i class="feather-linkedin text-primary me-1"></i>
                                @Html.T("رابط LinkedIn", "LinkedIn Profile")
                            </label>
                            <input asp-for="LinkedInUrl" class="form-control" placeholder="https://linkedin.com/in/..." />
                            <span asp-validation-for="LinkedInUrl" class="text-danger small"></span>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label asp-for="WebsiteUrl" class="form-label">
                                <i class="feather-globe text-success me-1"></i>
                                @Html.T("الموقع الشخصي", "Personal Website")
                            </label>
                            <input asp-for="WebsiteUrl" class="form-control" placeholder="https://yourwebsite.com" />
                            <span asp-validation-for="WebsiteUrl" class="text-danger small"></span>
                        </div>
                    </div>

                    <div class="row mb-4">
                        <div class="col-md-6 mb-3">
                            <label asp-for="YouTubeUrl" class="form-label">
                                <i class="feather-youtube text-danger me-1"></i>
                                @Html.T("قناة YouTube", "YouTube Channel")
                            </label>
                            <input asp-for="YouTubeUrl" class="form-control" placeholder="https://youtube.com/c/..." />
                            <span asp-validation-for="YouTubeUrl" class="text-danger small"></span>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label asp-for="SampleVideoUrl" class="form-label">
                                <i class="feather-video text-info me-1"></i>
                                @Html.T("فيديو تعريفي", "Sample Video")
                            </label>
                            <input asp-for="SampleVideoUrl" class="form-control" placeholder="@Html.T("رابط فيديو يعرض أسلوب تدريسك", "Link to a video showcasing your teaching style")" />
                            <span asp-validation-for="SampleVideoUrl" class="text-danger small"></span>
                        </div>
                    </div>

                    <!-- Terms and Submit -->
                    <div class="mb-4">
                        <div class="form-check">
                            <input type="checkbox" class="form-check-input" name="AcceptTerms" value="true" id="AcceptTerms" @(Model?.AcceptTerms == true ? "checked" : null) />
                            <label class="form-check-label" for="AcceptTerms">
                                @Html.T("أوافق على", "I agree to the") <a href="/Privacy" target="_blank" class="text-primary">@Html.T("الشروط والأحكام", "Terms & Conditions")</a> @Html.T("وسياسة الخصوصية", "and Privacy Policy")
                            </label>
                        </div>
                        <span asp-validation-for="AcceptTerms" class="text-danger small"></span>
                    </div>

                    <div class="d-grid gap-2 mt-4">
                        <button type="submit" class="btn btn-primary btn-lg">
                            <i class="feather-send me-2"></i>
                            @Html.T("تقديم الطلب", "Submit Application")
                        </button>
                    </div>

                    @if (!User.Identity?.IsAuthenticated ?? true)
                    {
                        <div class="text-center mt-4">
                            <p class="text-muted">
                                @Html.T("لديك حساب بالفعل؟", "Already have an account?") 
                                <a href="@Url.Action("Login", "Account")" class="text-primary fw-bold">@Html.T("تسجيل الدخول", "Sign In")</a>
                            </p>
                        </div>
                    }
                </form>
            </div>
        </div>
    </div>
</div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

