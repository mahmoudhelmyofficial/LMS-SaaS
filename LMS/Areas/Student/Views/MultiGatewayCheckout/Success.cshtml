@{
    ViewData["Title"] = Html.T("تم الدفع بنجاح", "Payment Successful");
    Layout = "_StudentLayout";
    var orderId = ViewBag.OrderId as string;
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card shadow-lg border-0">
                <div class="card-body text-center p-5">
                    <div class="mb-4">
                        <div class="rounded-circle bg-success text-white d-inline-flex align-items-center justify-content-center" 
                             style="width: 100px; height: 100px;">
                            <i class="fas fa-check fa-3x"></i>
                        </div>
                    </div>
                    
                    <h2 class="text-success mb-3">🎉 @Html.T("تهانينا!", "Congratulations!")</h2>
                    <h4 class="mb-4">@Html.T("تمت عملية الدفع بنجاح", "Payment completed successfully")</h4>
                    
                    @if (!string.IsNullOrEmpty(orderId))
                    {
                        <div class="bg-light rounded p-3 mb-4">
                            <small class="text-muted d-block">@Html.T("رقم الطلب", "Order number")</small>
                            <code class="fs-5">@orderId</code>
                        </div>
                    }
                    
                    <p class="text-muted mb-4">
                        @Html.T("تم تسجيلك بنجاح في الدورات/الكتب التي اشتريتها.", "You have been successfully enrolled in the courses/books you purchased.")
                        <br>
                        @Html.T("يمكنك البدء في التعلم الآن!", "You can start learning now!")
                    </p>
                    
                    <div class="d-grid gap-3">
                        <a href="@Url.Action("Index", "Courses", new { area = "Student" })" class="btn btn-primary btn-lg">
                            <i class="fas fa-book-open me-2"></i>
                            @Html.T("ابدأ التعلم الآن", "Start learning now")
                        </a>
                        <a href="@Url.Action("MyLibrary", "Books", new { area = "Student" })" class="btn btn-outline-primary">
                            <i class="fas fa-book me-2"></i>
                            @Html.T("مكتبتي", "My library")
                        </a>
                        <a href="@Url.Action("Browse", "Courses", new { area = "Student" })" class="btn btn-outline-secondary">
                            <i class="fas fa-search me-2"></i>
                            @Html.T("تصفح المزيد من الدورات", "Browse more courses")
                        </a>
                    </div>
                </div>
            </div>
            
            <!-- Helpful Tips -->
            <div class="card mt-4 border-0 shadow-sm">
                <div class="card-body">
                    <h5 class="mb-3">
                        <i class="fas fa-lightbulb text-warning me-2"></i>
                        @Html.T("نصائح للبدء", "Tips to get started")
                    </h5>
                    <ul class="list-unstyled mb-0">
                        <li class="mb-2">
                            <i class="fas fa-check-circle text-success me-2"></i>
                            @Html.T("تم إرسال تأكيد الشراء إلى بريدك الإلكتروني", "Purchase confirmation has been sent to your email")
                        </li>
                        <li class="mb-2">
                            <i class="fas fa-check-circle text-success me-2"></i>
                            @Html.T("يمكنك الوصول للدورات من أي جهاز", "Access your courses from any device")
                        </li>
                        <li class="mb-2">
                            <i class="fas fa-check-circle text-success me-2"></i>
                            @Html.T("شهادة إتمام عند إنهاء الدورة بنجاح", "Completion certificate when you finish the course")
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>

