@model LMS.Areas.Student.ViewModels.BankTransferStatusViewModel
@{
    ViewData["Title"] = Html.T("حالة التحويل البنكي", "Bank Transfer Status");
    Layout = "_StudentLayout";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card shadow-lg border-0">
                <div class="card-body text-center p-5">
                    @switch (Model.Status)
                    {
                        case "AwaitingProof":
                            <div class="mb-4">
                                <div class="rounded-circle bg-warning text-white d-inline-flex align-items-center justify-content-center"
                                     style="width: 100px; height: 100px;">
                                    <i class="fas fa-hourglass-half fa-3x"></i>
                                </div>
                            </div>
                            <h4 class="text-warning mb-3">@Html.T("في انتظار إثبات التحويل", "Awaiting transfer proof")</h4>
                            <p class="text-muted mb-4">
                                @Html.T("يرجى إتمام التحويل البنكي ورفع صورة الإيصال", "Please complete the bank transfer and upload the receipt image")
                            </p>
                            <a href="@Url.Action("BankTransferInstructions", new { reference = Model.ReferenceNumber })" 
                               class="btn btn-primary btn-lg">
                                <i class="fas fa-upload me-2"></i>
                                @Html.T("رفع إثبات الدفع", "Upload payment proof")
                            </a>
                            break;

                        case "ProofSubmitted":
                            <div class="mb-4">
                                <div class="rounded-circle bg-info text-white d-inline-flex align-items-center justify-content-center"
                                     style="width: 100px; height: 100px;">
                                    <i class="fas fa-clock fa-3x"></i>
                                </div>
                            </div>
                            <h4 class="text-info mb-3">@Html.T("تم تقديم الإثبات", "Proof submitted")</h4>
                            <p class="text-muted mb-4">
                                @Html.T("جاري مراجعة الدفعة من قبل الإدارة", "Your payment is being reviewed by the team")
                                <br />
                                @Html.T("سيتم إشعارك عند الانتهاء", "You will be notified when complete")
                            </p>
                            @if (!string.IsNullOrEmpty(Model.ProofImageUrl))
                            {
                                <div class="mb-3">
                                    <img src="@Model.ProofImageUrl" alt="@Html.T("إثبات الدفع", "Payment proof")" 
                                         class="img-thumbnail" style="max-width: 200px;">
                                </div>
                                <small class="text-muted d-block mb-3">
                                    @Html.T("تم التقديم:", "Submitted:") @Model.ProofSubmittedAt?.ToString("yyyy/MM/dd HH:mm")
                                </small>
                            }
                            break;

                        case "Approved":
                            <div class="mb-4">
                                <div class="rounded-circle bg-success text-white d-inline-flex align-items-center justify-content-center"
                                     style="width: 100px; height: 100px;">
                                    <i class="fas fa-check fa-3x"></i>
                                </div>
                            </div>
                            <h4 class="text-success mb-3">🎉 @Html.T("تم اعتماد الدفعة!", "Payment approved!")</h4>
                            <p class="text-muted mb-4">
                                @Html.T("تم التحقق من التحويل وتفعيل اشتراكاتك بنجاح", "Transfer verified and your subscriptions have been activated successfully")
                            </p>
                            <a href="@Url.Action("Index", "Courses", new { area = "Student" })" class="btn btn-success btn-lg">
                                <i class="fas fa-book-open me-2"></i>
                                @Html.T("ابدأ التعلم", "Start learning")
                            </a>
                            break;

                        case "Rejected":
                            <div class="mb-4">
                                <div class="rounded-circle bg-danger text-white d-inline-flex align-items-center justify-content-center"
                                     style="width: 100px; height: 100px;">
                                    <i class="fas fa-times fa-3x"></i>
                                </div>
                            </div>
                            <h4 class="text-danger mb-3">@Html.T("تم رفض الدفعة", "Payment rejected")</h4>
                            @if (!string.IsNullOrEmpty(Model.RejectionReason))
                            {
                                <div class="alert alert-danger text-start">
                                    <strong>@Html.T("السبب:", "Reason:")</strong> @Model.RejectionReason
                                </div>
                            }
                            <p class="text-muted mb-4">
                                @Html.T("يمكنك المحاولة مرة أخرى أو التواصل مع الدعم الفني", "You can try again or contact support")
                            </p>
                            <a href="@Url.Action("Index", "Checkout", new { area = "Student" })" class="btn btn-primary">
                                <i class="fas fa-redo me-2"></i>
                                @Html.T("إعادة المحاولة", "Try again")
                            </a>
                            break;

                        case "Expired":
                            <div class="mb-4">
                                <div class="rounded-circle bg-secondary text-white d-inline-flex align-items-center justify-content-center"
                                     style="width: 100px; height: 100px;">
                                    <i class="fas fa-clock fa-3x"></i>
                                </div>
                            </div>
                            <h4 class="text-secondary mb-3">@Html.T("انتهت صلاحية الطلب", "Order expired")</h4>
                            <p class="text-muted mb-4">
                                @Html.T("انتهت صلاحية هذا الطلب", "This order has expired")
                                <br />
                                @Html.T("يمكنك إنشاء طلب جديد", "You can create a new order")
                            </p>
                            <a href="@Url.Action("Cart", "Checkout", new { area = "Student" })" class="btn btn-primary">
                                <i class="fas fa-shopping-cart me-2"></i>
                                @Html.T("العودة للسلة", "Back to cart")
                            </a>
                            break;

                        default:
                            <div class="mb-4">
                                <div class="rounded-circle bg-secondary text-white d-inline-flex align-items-center justify-content-center"
                                     style="width: 100px; height: 100px;">
                                    <i class="fas fa-question fa-3x"></i>
                                </div>
                            </div>
                            <h4 class="text-secondary mb-3">@Html.T("حالة غير معروفة", "Unknown status")</h4>
                            break;
                    }

                    <hr class="my-4" />

                    <div class="d-flex justify-content-between text-muted small">
                        <span>@Html.T("رقم المرجع:", "Reference number:")</span>
                        <code>@Model.ReferenceNumber</code>
                    </div>
                    <div class="d-flex justify-content-between text-muted small">
                        <span>@Html.T("المبلغ:", "Amount:")</span>
                        <span>@Model.Amount.ToEGP(2)</span>
                    </div>
                </div>
            </div>

            <div class="text-center mt-4">
                <a href="@Url.Action("Index", "Dashboard", new { area = "Student" })" class="btn btn-outline-secondary">
                    <i class="fas fa-home me-1"></i>
                    @Html.T("العودة للرئيسية", "Back to home")
                </a>
            </div>
        </div>
    </div>
</div>

