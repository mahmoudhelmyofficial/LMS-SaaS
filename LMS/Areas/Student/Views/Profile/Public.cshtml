@model LMS.Areas.Student.ViewModels.PublicProfileViewModel
@{
    ViewData["Title"] = Model.User?.FullName ?? Html.T("الملف الشخصي", "Profile");
    Layout = "~/Areas/Student/Views/Shared/_Layout.cshtml";
    var stats = Model.Stats;
}

@section Styles {
<style>
    .public-profile-header {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        padding: 80px 0 120px;
        margin-bottom: -80px;
        position: relative;
    }
    .profile-card {
        background: white;
        border-radius: 16px;
        padding: 40px;
        box-shadow: 0 10px 40px rgba(0,0,0,0.1);
        position: relative;
        z-index: 1;
    }
    .profile-avatar-large {
        width: 150px;
        height: 150px;
        border-radius: 50%;
        border: 6px solid white;
        box-shadow: 0 8px 20px rgba(0,0,0,0.15);
        margin-top: -100px;
        object-fit: cover;
    }
    .stat-circle {
        width: 100px;
        height: 100px;
        border-radius: 50%;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
    }
    .badge-item {
        background: #f9fafb;
        border-radius: 12px;
        padding: 20px;
        text-align: center;
        transition: all 0.3s ease;
    }
    .badge-item:hover {
        transform: translateY(-5px);
        box-shadow: 0 8px 20px rgba(0,0,0,0.1);
    }
    .course-completed-item {
        border: 2px solid #e5e7eb;
        border-radius: 12px;
        padding: 20px;
        transition: all 0.3s ease;
    }
    .course-completed-item:hover {
        border-color: #22c55e;
        box-shadow: 0 4px 15px rgba(34, 197, 94, 0.1);
    }
    .skill-item {
        margin-bottom: 1rem;
    }
</style>
}

<!-- Public Profile Header -->
<div class="public-profile-header">
    <div class="container">
        <div class="text-center text-white">
            <h1 class="fw-bold mb-3">@Html.T("الملف الشخصي العام", "Public Profile")</h1>
            <p class="fs-5 opacity-75">@Html.T("إنجازات ومهارات", "Achievements & skills of") @Model.User?.FirstName</p>
        </div>
    </div>
</div>

<!-- Profile Card -->
<div class="container">
    <div class="profile-card mb-5">
        <div class="text-center">
            <!-- Profile Picture -->
            @if (!string.IsNullOrEmpty(Model.User?.ProfilePictureUrl))
            {
                <img src="@Model.User.ProfilePictureUrl" 
                     alt="@Model.User.FullName" 
                     class="profile-avatar-large" />
            }
            else
            {
                <div class="profile-avatar-large bg-gradient-primary d-flex align-items-center justify-content-center mx-auto">
                    <i class="feather-user text-white" style="font-size: 60px;"></i>
                </div>
            }

            <!-- Name & Bio -->
            <h2 class="fw-bold mt-4 mb-2">@Model.User?.FullName</h2>
            @if (!string.IsNullOrEmpty(Model.User?.Bio))
            {
                <p class="text-muted fs-5 mb-4 mx-auto" style="max-width: 600px;">@Model.User.Bio</p>
            }
            
            <!-- Member Since -->
            <div class="d-inline-flex align-items-center gap-2 px-4 py-2 bg-soft-primary text-primary rounded-pill">
                <i class="feather-calendar"></i>
                <span>@Html.T("عضو منذ", "Member since") @Model.User?.CreatedAt.ToString("MMMM yyyy")</span>
            </div>
        </div>

        <!-- Stats Row -->
        <div class="row g-4 mt-5">
            <div class="col-lg-3 col-md-6">
                <div class="text-center">
                    <div class="stat-circle mx-auto mb-3">
                        <div class="fs-2 fw-bold">@(stats?.CompletedCourses ?? 0)</div>
                        <small class="opacity-75">@Html.T("دورة", "Course")</small>
                    </div>
                    <h6 class="fw-bold mb-0">@Html.T("دورات مكتملة", "Completed Courses")</h6>
                </div>
            </div>
            <div class="col-lg-3 col-md-6">
                <div class="text-center">
                    <div class="stat-circle mx-auto mb-3" style="background: linear-gradient(135deg, #22c55e 0%, #16a34a 100%);">
                        <div class="fs-2 fw-bold">@(stats?.CertificatesEarned ?? 0)</div>
                        <small class="opacity-75">@Html.T("شهادة", "Certificate")</small>
                    </div>
                    <h6 class="fw-bold mb-0">@Html.T("شهادات محصلة", "Certificates Earned")</h6>
                </div>
            </div>
            <div class="col-lg-3 col-md-6">
                <div class="text-center">
                    <div class="stat-circle mx-auto mb-3" style="background: linear-gradient(135deg, #fbbf24 0%, #f59e0b 100%);">
                        <div class="fs-2 fw-bold">@(stats?.TotalBadges ?? 0)</div>
                        <small class="opacity-75">@Html.T("شارة", "Badge")</small>
                    </div>
                    <h6 class="fw-bold mb-0">@Html.T("شارات مكتسبة", "Badges Earned")</h6>
                </div>
            </div>
            <div class="col-lg-3 col-md-6">
                <div class="text-center">
                    <div class="stat-circle mx-auto mb-3" style="background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);">
                        <div class="fs-2 fw-bold">@(stats?.CurrentLevel ?? 1)</div>
                        <small class="opacity-75">@Html.T("مستوى", "Level")</small>
                    </div>
                    <h6 class="fw-bold mb-0">@Html.T("مستوى الخبرة", "Experience Level")</h6>
                </div>
            </div>
        </div>
    </div>

    <div class="row g-4 mb-5">
        <!-- Badges Section -->
        <div class="col-lg-6">
            <div class="card stretch stretch-full">
                <div class="card-header">
                    <h5 class="fw-bold mb-0">
                        <i class="feather-award text-warning me-2"></i>
                        @Html.T("الشارات والإنجازات", "Badges & Achievements")
                    </h5>
                </div>
                <div class="card-body">
                    @if (Model.Badges != null && Model.Badges.Any())
                    {
                        <div class="row g-3">
                            @foreach (var userBadge in Model.Badges.Take(4))
                            {
                                <div class="col-md-6">
                                    <div class="badge-item">
                                        <div class="fs-1 mb-2">@(userBadge.Badge?.IconEmoji ?? "🏆")</div>
                                        <h6 class="fw-bold mb-1">@userBadge.Badge?.Name</h6>
                                        <small class="text-muted">@userBadge.Badge?.Description</small>
                                    </div>
                                </div>
                            }
                        </div>
                    }
                    else
                    {
                        <div class="text-center py-4">
                            <i class="feather-award fs-1 text-muted mb-2 d-block"></i>
                            <p class="text-muted mb-0">@Html.T("لا توجد شارات بعد", "No badges yet")</p>
                        </div>
                    }
                </div>
            </div>
        </div>

        <!-- Certificates Section -->
        <div class="col-lg-6">
            <div class="card stretch stretch-full">
                <div class="card-header">
                    <h5 class="fw-bold mb-0">
                        <i class="feather-file-text text-primary me-2"></i>
                        @Html.T("الشهادات المحصلة", "Certificates Earned")
                    </h5>
                </div>
                <div class="card-body">
                    @if (Model.Certificates != null && Model.Certificates.Any())
                    {
                        @foreach (var certificate in Model.Certificates.Take(3))
                        {
                            <div class="course-completed-item mb-3">
                                <div class="d-flex align-items-center">
                                    <div class="avatar-text avatar-lg bg-soft-primary text-primary me-3">
                                        <i class="feather-award fs-3"></i>
                                    </div>
                                    <div class="flex-grow-1">
                                        <h6 class="fw-bold mb-1">@certificate.Course?.Title</h6>
                                        <p class="text-muted small mb-0">@Html.T("شهادة إتمام معتمدة", "Accredited completion certificate")</p>
                                    </div>
                                    <div class="text-success">
                                        <i class="feather-check-circle fs-4"></i>
                                    </div>
                                </div>
                            </div>
                        }
                    }
                    else
                    {
                        <div class="text-center py-4">
                            <i class="feather-file-text fs-1 text-muted mb-2 d-block"></i>
                            <p class="text-muted mb-0">@Html.T("لا توجد شهادات بعد", "No certificates yet")</p>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>

    <!-- Skills Progress -->
    @if (stats?.EarnedBadges != null && stats.EarnedBadges.Any())
    {
        <div class="card stretch stretch-full mb-5">
            <div class="card-header">
                <h5 class="fw-bold mb-0">
                    <i class="feather-trending-up text-info me-2"></i>
                    @Html.T("إحصائيات التعلم", "Learning Statistics")
                </h5>
            </div>
            <div class="card-body">
                <div class="row g-4">
                    <div class="col-md-3">
                        <div class="text-center p-3 bg-soft-primary rounded">
                            <h3 class="fw-bold text-primary mb-1">@(stats?.TotalStudyMinutes / 60)</h3>
                            <small class="text-muted">@Html.T("ساعة دراسة", "Study Hours")</small>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="text-center p-3 bg-soft-success rounded">
                            <h3 class="fw-bold text-success mb-1">@(stats?.TotalLessonsCompleted ?? 0)</h3>
                            <small class="text-muted">@Html.T("درس مكتمل", "Lessons Completed")</small>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="text-center p-3 bg-soft-warning rounded">
                            <h3 class="fw-bold text-warning mb-1">@(stats?.TotalQuizzesTaken ?? 0)</h3>
                            <small class="text-muted">@Html.T("اختبار", "Quizzes")</small>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="text-center p-3 bg-soft-info rounded">
                            <h3 class="fw-bold text-info mb-1">@(stats?.CurrentStreak ?? 0)</h3>
                            <small class="text-muted">@Html.T("يوم متتالي", "Day Streak")</small>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }

    <!-- Share Profile -->
    <div class="text-center mb-5">
        <h5 class="fw-bold mb-3">@Html.T("شارك هذا الملف", "Share This Profile")</h5>
        <div class="d-flex justify-content-center gap-3 flex-wrap">
            <button class="btn btn-outline-primary" onclick="shareProfile('twitter')">
                <i class="feather-twitter me-2"></i>
                @Html.T("تويتر", "Twitter")
            </button>
            <button class="btn btn-outline-primary" onclick="shareProfile('facebook')">
                <i class="feather-facebook me-2"></i>
                @Html.T("فيسبوك", "Facebook")
            </button>
            <button class="btn btn-outline-primary" onclick="shareProfile('linkedin')">
                <i class="feather-linkedin me-2"></i>
                @Html.T("لينكد إن", "LinkedIn")
            </button>
            <button class="btn btn-outline-secondary" onclick="copyProfileLink()">
                <i class="feather-link me-2"></i>
                @Html.T("نسخ الرابط", "Copy Link")
            </button>
        </div>
    </div>
</div>

@section Scripts {
<script>
    function shareProfile(platform) {
        var url = window.location.href;
        var text = '@Html.Raw(Html.T("شاهد ملفي التعليمي على منصة LMS", "Check out my learning profile on LMS"))';
        
        var shareUrl = '';
        switch(platform) {
            case 'twitter':
                shareUrl = 'https://twitter.com/intent/tweet?text=' + encodeURIComponent(text) + '&url=' + encodeURIComponent(url);
                break;
            case 'facebook':
                shareUrl = 'https://www.facebook.com/sharer/sharer.php?u=' + encodeURIComponent(url);
                break;
            case 'linkedin':
                shareUrl = 'https://www.linkedin.com/sharing/share-offsite/?url=' + encodeURIComponent(url);
                break;
        }
        
        if(shareUrl) {
            window.open(shareUrl, '_blank', 'width=600,height=400');
        }
    }

    function copyProfileLink() {
        var url = window.location.href;
        navigator.clipboard.writeText(url).then(function() {
            alert('@Html.Raw(Html.T("تم نسخ الرابط بنجاح!", "Link copied successfully!").Replace("'", "\\'"))');
        });
    }
</script>
}
