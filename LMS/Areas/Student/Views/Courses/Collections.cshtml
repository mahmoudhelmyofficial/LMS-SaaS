@model List<LMS.Domain.Entities.Learning.CourseCollection>
@{
    ViewData["Title"] = Html.T("مجموعات الدورات", "Course Collections");
}

@section Styles {
<style>
    .collection-card {
        border: 2px solid #e5e7eb;
        border-radius: 12px;
        padding: 24px;
        transition: all 0.3s ease;
        cursor: pointer;
        height: 100%;
    }
    .collection-card:hover {
        border-color: #667eea;
        transform: translateY(-5px);
        box-shadow: 0 8px 20px rgba(102, 126, 234, 0.2);
    }
    .collection-icon {
        width: 60px;
        height: 60px;
        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 28px;
        margin-bottom: 16px;
    }
    .course-mini-card {
        background: #f9fafb;
        border-radius: 8px;
        padding: 12px;
        margin-bottom: 8px;
        display: flex;
        align-items: center;
        gap: 12px;
    }
</style>
}

<!-- Alert Messages -->
@await Html.PartialAsync("_AlertMessage")

<!-- Page Header -->
<div class="card stretch stretch-full mb-4">
    <div class="card-body">
        <div class="d-flex justify-content-between align-items-center flex-wrap gap-3">
            <div>
                <h3 class="fw-bold mb-2">
                    <i class="feather-folder text-primary me-2"></i>
                    @Html.T("مجموعات الدورات", "Course Collections")
                </h3>
                <p class="text-muted mb-0">@Html.T("نظّم دوراتك في مجموعات مخصصة", "Organize your courses into custom collections")</p>
            </div>
            <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#createCollectionModal">
                <i class="feather-plus me-2"></i>
                @Html.T("مجموعة جديدة", "New Collection")
            </button>
        </div>
    </div>
</div>

<!-- Collections Grid -->
<div class="row g-4">
    @if (Model != null && Model.Any())
    {
        @foreach (var collection in Model)
        {
            var coursesCount = collection.Courses?.Count ?? 0;
            var totalHours = collection.Courses?.Sum(c => c.Course?.DurationHours ?? 0) ?? 0;
            // Calculate progress based on enrolled courses
            var completedCount = 0;
            var progressPercent = 0;
            
            <div class="col-lg-4 col-md-6">
                <div class="collection-card">
                    <div class="d-flex justify-content-between align-items-start mb-3">
                        <div class="collection-icon bg-soft-primary text-primary">
                            📚
                        </div>
                        <div class="dropdown">
                            <button class="btn btn-sm btn-icon" data-bs-toggle="dropdown">
                                <i class="feather-more-vertical"></i>
                            </button>
                            <ul class="dropdown-menu dropdown-menu-end">
                                <li>
                                    <a class="dropdown-item" href="#" data-bs-toggle="modal" data-bs-target="#editCollectionModal" 
                                       data-id="@collection.Id" data-name="@collection.Name" data-description="@collection.Description">
                                        <i class="feather-edit me-2"></i>@Html.T("تعديل", "Edit")
                                    </a>
                                </li>
                                <li>
                                    <form asp-action="DeleteCollection" asp-route-id="@collection.Id" method="post" class="d-inline">
                                        @Html.AntiForgeryToken()
                                        <button type="submit" class="dropdown-item text-danger" onclick="return confirm('@Html.Raw(Html.T("هل أنت متأكد من حذف هذه المجموعة؟", "Are you sure you want to delete this collection?").Replace("'", "\\'"))')">
                                            <i class="feather-trash-2 me-2"></i>@Html.T("حذف", "Delete")
                                        </button>
                                    </form>
                                </li>
                            </ul>
                        </div>
                    </div>
                    <h5 class="fw-bold mb-2">@collection.Name</h5>
                    <p class="text-muted small mb-3">@(collection.Description ?? Html.T("بدون وصف", "No description"))</p>
                    <div class="d-flex align-items-center gap-3 mb-3">
                        <div class="d-flex align-items-center gap-2">
                            <i class="feather-book-open text-primary"></i>
                            <span class="fw-semibold">@coursesCount</span>
                            <small class="text-muted">@Html.T("دورات", "courses")</small>
                        </div>
                        <div class="d-flex align-items-center gap-2">
                            <i class="feather-clock text-warning"></i>
                            <span class="fw-semibold">@totalHours</span>
                            <small class="text-muted">@Html.T("ساعة", "hours")</small>
                        </div>
                    </div>
                    @if (coursesCount > 0)
                    {
                        <a asp-action="CollectionDetails" asp-route-id="@collection.Id" class="btn btn-outline-primary w-100">
                            <i class="feather-eye me-2"></i>
                            @Html.T("عرض المجموعة", "View Collection")
                        </a>
                    }
                    else
                    {
                        <span class="text-muted small">
                            <i class="feather-info me-1"></i>
                            @Html.T("لا توجد دورات في هذه المجموعة", "No courses in this collection")
                        </span>
                    }
                </div>
            </div>
        }
    }

    <!-- Add New Collection Card -->
    <div class="col-lg-4 col-md-6">
        <div class="collection-card border-dashed text-center" style="border-style: dashed;" data-bs-toggle="modal" data-bs-target="#createCollectionModal">
            <div class="py-5">
                <div class="avatar-text avatar-xl bg-soft-primary text-primary mx-auto mb-3">
                    <i class="feather-plus fs-1"></i>
                </div>
                <h5 class="fw-bold mb-2">@Html.T("إنشاء مجموعة جديدة", "Create New Collection")</h5>
                <p class="text-muted mb-0">@Html.T("نظّم دوراتك في مجموعات مخصصة", "Organize your courses into custom collections")</p>
            </div>
        </div>
    </div>
</div>

@if (Model == null || !Model.Any())
{
    <div class="text-center py-5">
        <i class="feather-folder fs-1 text-muted mb-3 d-block"></i>
        <h5 class="text-muted">@Html.T("لا توجد مجموعات بعد", "No collections yet")</h5>
        <p class="text-muted">@Html.T("أنشئ مجموعتك الأولى لتنظيم دوراتك", "Create your first collection to organize your courses")</p>
    </div>
}

<!-- Create Collection Modal -->
<div class="modal fade" id="createCollectionModal" tabindex="-1" data-bs-backdrop="false">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">
                    <i class="feather-folder-plus me-2"></i>
                    @Html.T("إنشاء مجموعة جديدة", "Create New Collection")
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <form asp-action="CreateCollection" method="post">
                @Html.AntiForgeryToken()
                <div class="modal-body">
                    <div class="mb-3">
                        <label class="form-label fw-bold">@Html.T("اسم المجموعة", "Collection Name")</label>
                        <input type="text" class="form-control" name="name" placeholder="@Html.T("مثال: دورات البرمجة", "Example: Programming Courses")" required />
                    </div>
                    <div class="mb-3">
                        <label class="form-label fw-bold">@Html.T("الوصف (اختياري)", "Description (optional)")</label>
                        <textarea class="form-control" name="description" rows="3" placeholder="@Html.T("وصف مختصر للمجموعة...", "Brief description of the collection...")"></textarea>
                    </div>
                    <div class="form-check">
                        <input class="form-check-input" type="checkbox" name="isPublic" id="isPublic" />
                        <label class="form-check-label" for="isPublic">@Html.T("مجموعة عامة (يمكن للآخرين رؤيتها)", "Public collection (visible to others)")</label>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">@Html.T("إلغاء", "Cancel")</button>
                    <button type="submit" class="btn btn-primary">
                        <i class="feather-check me-2"></i>
                        @Html.T("إنشاء", "Create")
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Edit Collection Modal -->
<div class="modal fade" id="editCollectionModal" tabindex="-1" data-bs-backdrop="false">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">
                    <i class="feather-edit me-2"></i>
                    @Html.T("تعديل المجموعة", "Edit Collection")
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <form asp-action="UpdateCollection" method="post">
                @Html.AntiForgeryToken()
                <input type="hidden" name="id" id="editCollectionId" />
                <div class="modal-body">
                    <div class="mb-3">
                        <label class="form-label fw-bold">@Html.T("اسم المجموعة", "Collection Name")</label>
                        <input type="text" class="form-control" name="name" id="editCollectionName" required />
                    </div>
                    <div class="mb-3">
                        <label class="form-label fw-bold">@Html.T("الوصف (اختياري)", "Description (optional)")</label>
                        <textarea class="form-control" name="description" id="editCollectionDescription" rows="3"></textarea>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">@Html.T("إلغاء", "Cancel")</button>
                    <button type="submit" class="btn btn-primary">
                        <i class="feather-save me-2"></i>
                        @Html.T("حفظ التغييرات", "Save Changes")
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
<script>
    $(document).ready(function() {
        // Handle edit modal
        $('#editCollectionModal').on('show.bs.modal', function (event) {
            var button = $(event.relatedTarget);
            var id = button.data('id');
            var name = button.data('name');
            var description = button.data('description');
            
            $('#editCollectionId').val(id);
            $('#editCollectionName').val(name);
            $('#editCollectionDescription').val(description);
        });
    });
</script>
}
