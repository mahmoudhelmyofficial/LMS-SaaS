@model LMS.Areas.Instructor.ViewModels.CourseWizardViewModel

<div class="step-7-review">
    <!-- Completion Status -->
    <div class="form-section">
        <div class="row">
            <div class="col-md-4">
                <div class="completion-circle text-center p-4 rounded-3 @(Model.CompletionPercentage >= 100 ? "bg-soft-success" : "bg-soft-warning")">
                    <div class="progress-circle mx-auto mb-3" style="--progress: @(Model.CompletionPercentage * 3.6)deg">
                        <span class="progress-value">@Model.CompletionPercentage%</span>
                    </div>
                    <h5 class="fw-bold @(Model.CompletionPercentage >= 100 ? "text-success" : "text-warning")">
                        @if (Model.CompletionPercentage >= 100)
                        {
                            <i class="feather-check-circle me-2"></i>
                            @:جاهزة للنشر
                        }
                        else
                        {
                            <i class="feather-alert-circle me-2"></i>
                            @:تحتاج إلى إكمال
                        }
                    </h5>
                </div>
            </div>
            <div class="col-md-8">
                <h5 class="form-section-title">
                    <i class="feather-clipboard"></i>
                    قائمة التحقق
                </h5>
                <div class="checklist-grid row g-3">
                    @foreach (var item in Model.ReadinessChecklist)
                    {
                        <div class="col-md-6">
                            <div class="checklist-card d-flex align-items-center p-3 rounded-3 @(item.IsCompleted ? "bg-soft-success" : (item.IsRequired ? "bg-soft-danger" : "bg-soft-warning"))">
                                <span class="checklist-icon me-3 @(item.IsCompleted ? "text-success" : (item.IsRequired ? "text-danger" : "text-warning"))">
                                    <i class="feather-@(item.IsCompleted ? "check-circle" : (item.IsRequired ? "x-circle" : "alert-circle")) fs-4"></i>
                                </span>
                                <div class="flex-grow-1">
                                    <span class="fw-medium @(item.IsCompleted ? "text-decoration-line-through text-muted" : "")">
                                        @item.NameArabic
                                    </span>
                                    @if (item.IsRequired && !item.IsCompleted)
                                    {
                                        <span class="badge bg-danger ms-2">مطلوب</span>
                                    }
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>

    <!-- Course Summary -->
    <div class="form-section">
        <h5 class="form-section-title">
            <i class="feather-file-text"></i>
            ملخص الدورة
        </h5>

        <div class="row g-4">
            <!-- Course Card Preview -->
            <div class="col-md-4">
                <div class="course-card-preview card border">
                    <div class="position-relative">
                        @if (!string.IsNullOrEmpty(Model.ThumbnailUrl))
                        {
                            <img src="@Model.ThumbnailUrl" class="card-img-top" style="aspect-ratio: 16/9; object-fit: cover;" alt="صورة الدورة">
                        }
                        else
                        {
                            <div class="bg-light d-flex align-items-center justify-content-center" style="aspect-ratio: 16/9;">
                                <i class="feather-image text-muted fs-1"></i>
                            </div>
                        }
                        @if (Model.IsFree)
                        {
                            <span class="badge bg-success position-absolute top-0 end-0 m-2">مجانية</span>
                        }
                    </div>
                    <div class="card-body">
                        <h6 class="fw-bold mb-2 text-truncate-2">@Model.Title</h6>
                        <p class="text-muted fs-12 mb-2 text-truncate-3">@Model.ShortDescription</p>
                        <div class="d-flex justify-content-between align-items-center">
                            @if (Model.IsFree)
                            {
                                <span class="text-success fw-bold">مجانية</span>
                            }
                            else if (Model.DiscountPrice.HasValue && Model.DiscountPrice < Model.Price)
                            {
                                <div>
                                    <span class="text-primary fw-bold">@Model.DiscountPrice.Value.ToString("N0") @Model.Currency</span>
                                    <span class="text-muted text-decoration-line-through ms-1 fs-12">@Model.Price.ToString("N0")</span>
                                </div>
                            }
                            else
                            {
                                <span class="text-primary fw-bold">@Model.Price.ToString("N0") @Model.Currency</span>
                            }
                            <span class="badge bg-soft-primary text-primary">
                                @(Model.Level switch
                                {
                                    LMS.Domain.Enums.CourseLevel.Beginner => "مبتدئ",
                                    LMS.Domain.Enums.CourseLevel.Intermediate => "متوسط",
                                    LMS.Domain.Enums.CourseLevel.Advanced => "متقدم",
                                    _ => "جميع المستويات"
                                })
                            </span>
                        </div>
                    </div>
                </div>
                <p class="text-center text-muted fs-12 mt-2">معاينة كرت الدورة</p>
            </div>

            <!-- Course Details -->
            <div class="col-md-8">
                <div class="summary-details">
                    <table class="table table-borderless">
                        <tbody>
                            <tr>
                                <th class="text-muted" style="width: 150px;">العنوان:</th>
                                <td class="fw-bold">@Model.Title</td>
                            </tr>
                            <tr>
                                <th class="text-muted">المستوى:</th>
                                <td>
                                    @(Model.Level switch
                                    {
                                        LMS.Domain.Enums.CourseLevel.Beginner => "مبتدئ",
                                        LMS.Domain.Enums.CourseLevel.Intermediate => "متوسط",
                                        LMS.Domain.Enums.CourseLevel.Advanced => "متقدم",
                                        _ => "جميع المستويات"
                                    })
                                </td>
                            </tr>
                            <tr>
                                <th class="text-muted">اللغة:</th>
                                <td>
                                    @(Model.Language == "ar" ? "العربية" : Model.Language == "en" ? "English" : Model.Language)
                                </td>
                            </tr>
                            <tr>
                                <th class="text-muted">التسعير:</th>
                                <td>
                                    @if (Model.IsFree)
                                    {
                                        <span class="badge bg-success">مجانية</span>
                                    }
                                    else
                                    {
                                        <span>@Model.Price.ToString("N2") @Model.Currency</span>
                                        @if (Model.DiscountPrice.HasValue)
                                        {
                                            <span class="text-success ms-2">(خصم: @Model.DiscountPrice.Value.ToString("N2"))</span>
                                        }
                                    }
                                </td>
                            </tr>
                            <tr>
                                <th class="text-muted">المحتوى:</th>
                                <td>
                                    <span class="badge bg-soft-primary text-primary me-2">@Model.TotalModulesCount وحدة</span>
                                    <span class="badge bg-soft-success text-success">@Model.TotalLessonsCount درس</span>
                                </td>
                            </tr>
                            <tr>
                                <th class="text-muted">نقاط التعلم:</th>
                                <td>@(Model.LearningOutcomes?.Count(o => !string.IsNullOrWhiteSpace(o)) ?? 0) نقطة</td>
                            </tr>
                            <tr>
                                <th class="text-muted">الميزات:</th>
                                <td>
                                    @if (Model.HasCertificate)
                                    {
                                        <span class="badge bg-soft-success text-success me-1">شهادة</span>
                                    }
                                    @if (Model.AllowDiscussions)
                                    {
                                        <span class="badge bg-soft-info text-info me-1">مناقشات</span>
                                    }
                                    @if (Model.AllowReviews)
                                    {
                                        <span class="badge bg-soft-warning text-warning me-1">تقييمات</span>
                                    }
                                    @if (Model.EnableContentDrip)
                                    {
                                        <span class="badge bg-soft-secondary text-secondary me-1">جدولة</span>
                                    }
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <!-- Learning Outcomes & Requirements Summary -->
    @if ((Model.LearningOutcomes?.Any() == true) || (Model.Requirements?.Any() == true))
    {
        <div class="form-section">
            <div class="row g-4">
                @if (Model.LearningOutcomes?.Any(o => !string.IsNullOrWhiteSpace(o)) == true)
                {
                    <div class="col-md-6">
                        <h6 class="fw-bold mb-3">
                            <i class="feather-check-circle text-success me-2"></i>
                            ما سيتعلمه الطالب
                        </h6>
                        <ul class="list-unstyled">
                            @foreach (var outcome in Model.LearningOutcomes.Where(o => !string.IsNullOrWhiteSpace(o)))
                            {
                                <li class="d-flex align-items-start mb-2">
                                    <i class="feather-check text-success me-2 mt-1"></i>
                                    <span>@outcome</span>
                                </li>
                            }
                        </ul>
                    </div>
                }
                @if (Model.Requirements?.Any(r => !string.IsNullOrWhiteSpace(r)) == true)
                {
                    <div class="col-md-6">
                        <h6 class="fw-bold mb-3">
                            <i class="feather-alert-circle text-warning me-2"></i>
                            المتطلبات
                        </h6>
                        <ul class="list-unstyled">
                            @foreach (var req in Model.Requirements.Where(r => !string.IsNullOrWhiteSpace(r)))
                            {
                                <li class="d-flex align-items-start mb-2">
                                    <i class="feather-arrow-left text-warning me-2 mt-1"></i>
                                    <span>@req</span>
                                </li>
                            }
                        </ul>
                    </div>
                }
            </div>
        </div>
    }

    <!-- Publish Action -->
    <div class="form-section">
        <div class="card border-0 @(Model.CompletionPercentage >= 100 ? "bg-gradient-success" : "bg-gradient-warning") text-white">
            <div class="card-body text-center py-5">
                @if (Model.CompletionPercentage >= 100)
                {
                    <div class="avatar-text avatar-xl bg-white text-success mx-auto mb-4">
                        <i class="feather-send fs-1"></i>
                    </div>
                    <h4 class="fw-bold mb-2">دورتك جاهزة للنشر! 🎉</h4>
                    <p class="opacity-75 mb-4">جميع المتطلبات مكتملة. انقر على "إرسال للمراجعة" لنشر دورتك.</p>
                    <div class="alert bg-white-10 border-0 text-white d-inline-block">
                        <i class="feather-info me-2"></i>
                        ستتم مراجعة الدورة خلال 24-48 ساعة
                    </div>
                }
                else
                {
                    <div class="avatar-text avatar-xl bg-white text-warning mx-auto mb-4">
                        <i class="feather-alert-triangle fs-1"></i>
                    </div>
                    <h4 class="fw-bold mb-2">أكمل المتطلبات الناقصة</h4>
                    <p class="opacity-75 mb-4">لا يمكن نشر الدورة حتى يتم إكمال جميع العناصر المطلوبة.</p>
                    
                    <div class="missing-items bg-white-10 rounded-3 p-3 text-start d-inline-block">
                        <h6 class="fw-bold mb-2">العناصر الناقصة:</h6>
                        <ul class="list-unstyled mb-0">
                            @foreach (var item in Model.ReadinessChecklist.Where(i => i.IsRequired && !i.IsCompleted))
                            {
                                <li class="mb-1">
                                    <i class="feather-x me-2"></i>
                                    @item.NameArabic
                                </li>
                            }
                        </ul>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

<style>
    .bg-gradient-success {
        background: linear-gradient(135deg, #059669 0%, #10b981 100%);
    }

    .bg-gradient-warning {
        background: linear-gradient(135deg, #d97706 0%, #f59e0b 100%);
    }

    .bg-gradient-primary {
        background: var(--gradient-primary, linear-gradient(135deg, #004aad 0%, #0066cc 50%, #3385d6 100%));
    }

    .bg-white-10 {
        background: rgba(255, 255, 255, 0.1);
    }

    .progress-circle {
        width: 120px;
        height: 120px;
        border-radius: 50%;
        background: conic-gradient(currentColor var(--progress), #e5e7eb var(--progress));
        display: flex;
        align-items: center;
        justify-content: center;
        position: relative;
    }

    .progress-circle::before {
        content: '';
        width: 100px;
        height: 100px;
        background: inherit;
        border-radius: 50%;
        position: absolute;
        background: white;
    }

    .progress-value {
        position: relative;
        z-index: 1;
        font-size: 1.5rem;
        font-weight: 700;
    }

    .bg-soft-success .progress-circle {
        color: #10b981;
    }

    .bg-soft-warning .progress-circle {
        color: #f59e0b;
    }

    .text-truncate-2 {
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }

    .text-truncate-3 {
        display: -webkit-box;
        -webkit-line-clamp: 3;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }

    .course-card-preview {
        border-radius: 12px;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .course-card-preview:hover {
        transform: translateY(-5px);
        box-shadow: 0 5px 20px rgba(0, 0, 0, 0.1);
    }

    .checklist-card {
        border-radius: 8px;
        transition: all 0.2s ease;
    }

    .checklist-card:hover {
        transform: translateX(-5px);
    }

    .completion-circle {
        border-radius: 12px;
    }

    .checklist-grid .col-md-6 {
        margin-bottom: 0.5rem;
    }
</style>

